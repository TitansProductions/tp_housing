**TP Housing**

# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added the possibility when a location is about to set (wardrobe, storage, etc), to also have an extra option to cancel action.
2. Added in the config (2) new options that allow you to open storage or wardrobe through the menu than having locations with prompts. This feature is great for small properties or for the ones who don't really want a property to have different positions for every actions.
3. Added new UI which is a new replacement of the `tpz_menu_base` requirement - this script will no longer be a requirement.
4. Added voice-talk support for the new UI that replaces `tpz_menu_base` in order to support voice chat while NUI is active.
5. Added an extra distance check to prevent wardrobe, storage and other positions to prevent them from being set near the menu prompt.
6. Added possibility to pay the taxes through TP Banks (Our paid script).
7. Added extra permissions for keyholder members.
8. Added on upgrades a new feature of increasing the default keyholder members by upgrading a property - totally optional.
9. Added on upgrades a new feature of the total property furniture placement based on the property upgrade level. There was a maximum furniture placement on `config_furnitures.lua` file, but this feature is now moved to upgrades.
10. Added a new database table column called `materials` which is going to contain all the materials you are going to use for upgrades.
11. Added possibility to allow tax failure attempts as an optional feature. This feature allows a specific limit of failed tax payments for preventing players to lose a property until it reaches a cap / maximum warnings. When a tax has failed to be paid, there is also an extra option to increase the next upcoming tax payment price because of failure.
12. Removed the possibility to buy furnitures through a location store, the only possible way is by buying furniture from the property nui itself or having your own by using items system.
13. Fixed an issue on deposit locale that mentions the limit of a property deposit, the locale was broken.
14. Removed `furniture` database table column from `characters` or `players` database table (depends what framework you are using) and now all purchased furnitures, are added directly to the property instead of the player who bought them.
15. Fixed an issue on placed furnitures, their position could become broken sometimes (not often).
16. Renamed all client and server class / files. They used to be named as `tp-server` and `tp-client`, they are now do not contain such indexes.
17. Fixed furniture models (Load Function) support for also using (`""`), it used to accept only numbers and hashes for furnitures. Example, a hash model required the hash or the number of the model (`-48905921`, ``` `p_chaircomfy11x` ```) but now you can also use `"p_chaircomfy11x"`.

## Modified Files:

- client folder **full replacement.**
- server directory folder **full replacement.**
- html **full replacement.**
- locales.lua **full replacement.**
- config/config.lua
- config/config_furniture.lua
- config/config_upgrades.lua
- fxmanifest.lua

In general, keep only config files, fxmanifest, client, server, html and locales, all must be deleted and replaced to latest.

## Notes:

1. Add the new configuration options on `config/config.lua` file related to storage and wardrobe menu access.

```lua
-- Set to false if you want the wardrobe access to be through a location instead through the menu.
Config.DisableWardobePositionSet = true -- 1.3.2

-- Set to false if you want the storage access to be through a location instead through the menu.
Config.DisableStoragePositionSet = true -- 1.3.2

-- Set to true if you are NOT using saltychat / teamspeak but you are using another voice script that blocks NUI voice, if you set it to true
-- it will enable a system which allows the players to hear you while you are having the NUI active.
Config.PushToTalkFunction = true -- 1.3.2

-- (!) Checkout client/escrow_allow.lua for adding more keys manually.
Config.HorseWhistleKey = 0x24978A28 -- (H BY DEFAULT) 1.3.2

-- (!) Checkout client/escrow_allow.lua for adding more keys manually.
Config.WagonWhistleKey = 0xF3830D8E --  (J BY DEFAULT) 1.3.2

-- Set to true if you are having TP Banks paid script to pay taxes directly through Banking Account.
Config.tp_banks = false -- 1.3.2
```

2. Add on `config/config_furniture.lua` above the `Furniture.Categories` configuration option a new option:

```lua
-- The camera view and objects position when clicking on "view" button for buying furnitures through the property nui.
Furnitures.PropertiesCameraView = { -- 1.3.2
	ObjectCoords = { x = -814.403076171875, y = -1374.77685546875, z = 43.72756958007812 },
	CameraCoords = { x = -811.915, y = -1376.60, z = 44.523, rotx = 0.0, roty = 0.0, rotz = 89.447189331055, zoom = 105.0 },
}
```

3. Add the new configuration options on every upgrade on `config/config_upgrades.lua` below `ExtraStorageWeight` option:

```lua
-- This is increasing the limit of @Config.MaxHouseKeyHolders, if you insert more than 0, this will allow extra keyholder members
-- for the specified property, which means if your input is 1, the calculation will be @Config.MaxHouseKeyHolders + 1
 ExtraPropertyKeyholders = 0, -- 1.3.2

-- How many furnitures should the property be able to have on the specified upgrade level?
-- This does NOT add extra furniture limit on current limit, it is THE limit - meaning, if input is (2), the property can only have (2) furnitures in total.
MaximumPropertyFurniturePlacement = 2, -- 1.3.2

BenefitsLabel1 = '• Maximum Furniture (2)\n• Storage Weight +10', -- This is when NOT having any extra keyholders (1.3.2)
BenefitsLabel2 = '• Maximum Furniture (2)\n• Storage Weight +10\n• Keyholder Members +?', -- This is when HAVING extra keyholders (1.3.2)
```

**CHECKOUT `config/config_upgrades.lua` FILE, EVERY UPGRADE HAS DIFFERENT FURNITURE PLACEMENT LIMIT AND KEYHOLDERS.**

4. Add the new thread on `client/escrow_allow.lua` to block key actions when having NUI active - this is required for those who have `Config.PushToTalkFunction` active.

```lua
-----------------------------------------------------------
--[[ Threads ]]--
-----------------------------------------------------------

if Config.PushToTalkFunction then
    -- The following task is made to prevent opening specific keys while nui is active.
    Citizen.CreateThread(function()
        while true do 
            
            local sleep  = 1500
        
            local player = PlayerPedId()
            local isDead = IsEntityDead(player)
    
            if not GetPlayerData().Loaded then
                goto END
            end
    
            if GetPlayerData().IsInMenu then 
                sleep = 0
    
                DisableControlAction(0, Config.HorseWhistleKey, true) -- Horse Whistle
                DisableControlAction(0, Config.WagonWhistleKey, true) -- Wagon Whistle
                
                DisableControlAction(0, 0xE6F612E4, true) -- 1
                DisableControlAction(0, 0x1CE6D9EB, true) -- 2
                DisableControlAction(0, 0x4F49CC4C, true) -- 3
                DisableControlAction(0, 0x8F9F9E58, true) -- 4
                DisableControlAction(0, 0xAB62E997, true) -- 5
                DisableControlAction(0, 0xA1FDE2A6, true) -- 6
                DisableControlAction(0, 0xB03A913B, true) -- 7
                DisableControlAction(0, 0x42385422, true) -- 8
    
                -- We close the NUI if player is dead.
                if isDead then 
                    CloseNUIProperly()
                end
    
            end
    
            ::END:: 
            Wait(sleep)
    
        end
    
    end)
end
```

5. Replace `Config.ManagementMenu` to the new one for supporting the UI that replaced the tpz_menu_base script.

```lua

-- FROM

-- Options in the property management menu to display / not based on your personal and server preferences.
-- (!) DO NOT MODIFY THE TYPE NAMES.
Config.ManagementMenu = {
    { Type = 'MENU_UPGRADES',          Enabled = true  }, -- upgrades menu option
    { Type = 'MENU_FURNITURE',         Enabled = true}, -- furniture placement and overview menu option
    { Type = 'MENU_WARDROBE_LOCATION', Enabled = true}, -- set wardrobe location menu option
    { Type = 'MENU_STORAGE_LOCATION',  Enabled = true}, -- set storage location menu option
    { Type = 'MENU_LEDGER',            Enabled = true }, -- deposit or withdraw money from the ledger menu option
    { Type = 'MENU_SET_KEYHOLDERS',    Enabled = true}, -- set property keyholders (members to have access to the house) menu option
    { Type = 'MENU_TRANSFER',          Enabled = true}, -- transfer the property to another player menu option
    { Type = 'MENU_SELL',              Enabled = true}, -- sell the property menu option
}

-- TO

-- Options in the property management menu to display / not based on your personal and server preferences.
-- (!) DO NOT MODIFY THE TYPE NAMES.
-- @param BackgroundImage : Images located at html/img/management folder (version 1.3.2)
Config.ManagementMenu = {
    { Type = 'MENU_UPGRADES',          Enabled = true, BackgroundImage = 'upgrades.jpg' }, -- upgrades menu option
    { Type = 'MENU_FURNITURE',         Enabled = true, BackgroundImage = 'furnitures.jpg'  }, -- furniture placement and overview menu option
    { Type = 'MENU_WARDROBE_LOCATION', Enabled = true, BackgroundImage = 'wardrobe.jpg'  }, -- set wardrobe location menu option
    { Type = 'MENU_STORAGE_LOCATION',  Enabled = true, BackgroundImage = 'storage.jpg'  }, -- set storage location menu option
    { Type = 'MENU_LEDGER',            Enabled = true, BackgroundImage = 'ledger.jpg'  }, -- deposit or withdraw money from the ledger menu option
    { Type = 'MENU_SET_KEYHOLDERS',    Enabled = true, BackgroundImage = 'keyholders.jpg'  }, -- set property keyholders (members to have access to the house) menu option
    { Type = 'MENU_TRANSFER',          Enabled = true, BackgroundImage = 'transfer.jpg'  }, -- transfer the property to another player menu option
    { Type = 'MENU_SELL',              Enabled = true, BackgroundImage = 'sell.jpg' }, -- sell the property menu option
}
```

6. Add the new database columns:

```sql
ALTER TABLE `tp_properties`
    ADD COLUMN IF NOT EXISTS `materials` LONGTEXT NOT NULL DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS `banking` INT(1) NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS `tax_warnings` INT(1) NOT NULL DEFAULT 0;
```

7. Add the (2) new config options related to materials for upgrades on `config/config_upgrades.lua`

```lua

-- Insert below all the materials that are going to be used for upgrades.
Config.AllowlistedUpgradeMaterials = { -- 1.3.2

    ['wooden_nails'] = { Label = 'Wooden Nails', MaximumQuantity = 100 },
    ['wood']         = { Label = 'Wood Log',      MaximumQuantity = 100 },
    ['rock']         = { Label = 'Rocks',         MaximumQuantity = 100 },
    ['fibers']       = { Label = 'Rocks',         MaximumQuantity = 100 },
    ['oilunrefined'] = { Label = 'Unrefined Oil', MaximumQuantity = 100 },
    ['ironbar']      = { Label = 'Ironbar',       MaximumQuantity = 100 },
    ['copper']       = { Label = 'Copper',        MaximumQuantity = 100 },
    ['metal']        = { Label = 'Metal',         MaximumQuantity = 100 },
    ['nails']        = { Label = 'Metal Nails',   MaximumQuantity = 100 },
    ['steel']        = { Label = 'Steel',         MaximumQuantity = 100 },
    ['leather']      = { Label = 'Leather',       MaximumQuantity = 100 },
    ['clay']         = { Label = 'Clay',          MaximumQuantity = 100 },

}

-- Inventory path is required for loading item images directly from your inventory and not from our script.
-- TPZ-CORE : tpz_inventory/html/img/items/
-- VORP: vorp_inventory/html/img/items/
-- RSG: rsg-inventory/html/images/
-- (!) OX Inventory will never work, they using web connections and blocking our connection.
Config.InventoryImgPath = "vorp_inventory/html/img/items/" -- version(1.3.2)
```

8. Add the new configuration options related to property taxes on `Config.TaxRepoSystem` option.

```lua
-- Set to 0 if you don't want to add any warnings to a property for failing a tax payment.
-- A warning is used to not remove a property but to increase the next tax payment fare and when reaching the cap,
 -- to completely remove the ownership if there are no payments.
Warnings        = 3, -- 3.1.2

-- How much would you like to increase a tax payment when failed? For example if a tax payment costs 30 dollars, 
-- and you add 10%, the tax will become 33 dollars to be paid.
IncreaseFailedTaxPercent = 10, -- Set to 0 to disable increasing the next tax payment (1.3.2)
```

Example:

```lua
Config.TaxRepoSystem = {
    Enabled = true,

    UpdateDuration  = 10, -- The update time in minutes.
    PaymentDuration = 14, -- By default, every 14 days for the repo tax payment.

    -- Set to 0 if you don't want to add any warnings to a property for failing a tax payment.
    -- A warning is used to not remove a property but to increase the next tax payment fare and when reaching the cap,
    -- to completely remove the ownership if there are no payments.
    Warnings        = 3, -- 3.1.2

    -- How much would you like to increase a tax payment when failed? For example if a tax payment costs 30 dollars, 
    -- and you add 10%, the tax will become 33 dollars to be paid.
    IncreaseFailedTaxPercent = 10, -- Set to 0 to disable increasing the next tax payment (1.3.2)

}
```

9. Add on `config/config_furniture.lua` where `Furnitures.Categories` section is located, the new option `BackgroundImage` on every category below below every **Category** name. You must add them manually unfortunately by checking the latest `config_furniture.lua` file (Search for 1.3.2)


## Screenshots
<img width="939" height="816" alt="εικόνα" src="https://github.com/user-attachments/assets/76926acf-545d-48aa-9f74-2e082eae1463" />
<img width="931" height="802" alt="εικόνα" src="https://github.com/user-attachments/assets/274c5e8e-1965-41c9-83b4-e2889bdcd2c5" />
<img width="907" height="821" alt="εικόνα" src="https://github.com/user-attachments/assets/00a3f12d-7e39-427c-b7b4-3a0b56d41610" />
<img width="951" height="859" alt="εικόνα" src="https://github.com/user-attachments/assets/56211dea-3ffb-40b0-9a27-5600c802e941" />
<img width="920" height="829" alt="εικόνα" src="https://github.com/user-attachments/assets/cab95108-3b69-4ea5-9ea8-dbf2e6913467" />
<img width="895" height="812" alt="εικόνα" src="https://github.com/user-attachments/assets/acec661c-cd98-461b-a93a-eb46d9e07950" />
<img width="884" height="827" alt="εικόνα" src="https://github.com/user-attachments/assets/2019d42a-4002-4ff0-81f1-535c2fcc803b" />
<img width="893" height="802" alt="εικόνα" src="https://github.com/user-attachments/assets/f34629df-ba9a-4e6e-be47-c27a524fb465" />
<img width="888" height="796" alt="εικόνα" src="https://github.com/user-attachments/assets/614b6a8d-56d4-4056-8ec9-7365064473f1" />
<img width="892" height="804" alt="εικόνα" src="https://github.com/user-attachments/assets/36c4a748-b452-4067-bdeb-30c0daab2a16" />
<img width="948" height="809" alt="εικόνα" src="https://github.com/user-attachments/assets/356260f7-d452-44be-9dc3-3de7351470c5" />
<img width="921" height="819" alt="εικόνα" src="https://github.com/user-attachments/assets/f2cbf952-2c1c-4dd9-8143-63c437ce8b92" />
<img width="910" height="812" alt="εικόνα" src="https://github.com/user-attachments/assets/9f96f5bc-12d9-483b-bf20-8f6040414dc5" />
<img width="1761" height="909" alt="εικόνα" src="https://github.com/user-attachments/assets/05bb246a-7a7c-4ec0-83ab-6ce42e27a84b" />
<img width="1559" height="817" alt="εικόνα" src="https://github.com/user-attachments/assets/d0160b37-10ad-4341-aabd-146fc317040d" />

